export default function LearnerDashboard() {
  return (
    <div className="max-w-4xl mx-auto p-8">
      <h1 className="text-2xl font-semibold text-brand-700">Learner</h1>
      <p className="text-sm text-gray-500 mt-2">
        Placeholder dashboard - this role's real screens (per the build brief's route list)
        land in later phases. This page exists to prove the login + RBAC + routing loop works
        end to end for this role.
      </p>
    </div>
  );
}
