node_modules
dist
build
.env
*.log
.DS_Store

# The Claude desktop app writes session outputs (screenshots etc.) here; not part of the app.
Claude outputs/
*.bundle

# Block 8b: recordings kept on the local filesystem when object storage is not configured
server/data/
