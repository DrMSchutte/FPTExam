# FPT Exam

FPT Academy's Secure Online Exam Centre

## Functional & Technical Specification — Proctored FISA / EISA Exams Platform

Prepared for: FPT Academy
Date: 29 August 2026 · **Revised 4 September 2026**
Status: v2 — reflects the decisions of 4 Sep 2026 (four roles; moderation/QA moved to FPTStaff; assessor-only sign-off; FPTStaff people/results integration). Target platform: Replit.

> **Revision note (4 Sep 2026).** This version supersedes v1 on the role model, sign-off gate, user registration and moderation. The detailed decision record is `moderation-signoff-policy.md`; the phase plan is `build-roadmap.md`. Where anything here still disagrees with those two, they win.

---

## 1. Purpose and Scope

FPT Academy requires a secure, proctored online exam centre to administer **Final Integrated Summative Assessments (FISA)** and **External Integrated Summative Assessments (EISA)** for QCTO-registered occupational qualifications, built and completed entirely online. The platform must let assessors and invigilators supervise learners remotely in real time, capture visual evidence of the exam session (webcam + periodic screen captures, with optional full recording), store that evidence securely, and automatically assess each learner's submission — generating a mark/competency recommendation and per-question feedback — with **the Assessor's sign-off as the single gate** before a result (and its feedback) becomes official and visible to the learner. Moderation and verification are **not** part of this platform: once a mark is signed off, the result is pushed to FPT Academy's **FPTStaff** application, which is where the QA process (moderation of the process portfolio, and verification for passed learners) runs.

This document is a build specification, not code. It is written so it can be handed to a developer (or to Replit's Agent/Assistant) as a build brief, and so FPT Academy's team has a single reference for what the system must do, who is allowed to do what, and which regulatory obligations shape those decisions.

Because "FISA" is FPT Academy's working term, a regulatory note up front: QCTO's own terminology splits this by qualification type — **FISA (Final Integrated Summative Assessment)** applies to legacy/historically registered qualifications, while **EISA (External Integrated Summative Assessment)** applies to qualifications registered directly under QCTO's occupational framework. Confirm with your AQP (Assessment Quality Partner) which term applies to each qualification you run through this platform — the workflow below works for both; the document uses "FISA/EISA" or just "the final assessment" throughout.

## 2. Regulatory Foundation (what the design is constrained by)

The platform's role model, invigilation rules, and sign-off flow are built around QCTO's Assessment Policy and the FISA/EISA writing guidelines, plus South Africa's POPIA for anything involving recorded video, screen capture, or biometric-adjacent data. Three points from that research drive specific design decisions below:

- **QCTO's assessment policy has no provision for remote/online invigilation** — it was written assuming physical assessment centres. This means FPT Academy is not implementing a QCTO-mandated remote process; it is building a platform that must *reproduce, in a defensible and auditable way*, the identity-verification and no-fraudulent-activity guarantees the policy expects of an in-person invigilator. Treat every proctoring feature below as evidence you can show an AQP auditor, not as a QCTO-specified feature.
- **QCTO/AQP practice separates roles by function and by independence**: Assessment Specialists (assessors) develop/conduct assessment; moderation checks that the process was fair, valid, reliable and unbiased; invigilators are procedural (identity + no fraudulent activity), not subject-matter roles, and guidelines for INSETA's FISA/EISA process explicitly require invigilators **not be employees of the assessment centre** for external assessments. The platform's role/permission design makes this separation enforceable (an Invigilator account cannot also be the Assessor of record on the same exam sitting), and lets the Administrator flag a given exam as requiring an *independent* (non-FPT-employed) invigilator. Moderation itself is performed in FPTStaff, on the process portfolio, not per result (Section 4a).
- **POPIA governs everything captured**: webcam video and screen recordings of a learner are personal information, and video capturing a person's face is treated as sensitive/biometric-adjacent processing requiring explicit, specific, informed consent, data minimisation, a defined retention period, and honouring access/correction/deletion requests. This is addressed throughout Sections 6, 9 and 10, and must be signed off by whoever handles compliance for FPT Academy before go-live — this document is not legal advice.

Sources consulted: [QCTO Assessment Policy for Qualifications and Part Qualifications](https://www.qcto.org.za/assets/qctos_-policy-on-assessment-of-qualifications-(3).pdf), [Guidelines for Writing of FISA & EISA (INSETA, 2018)](https://www.inseta.org.za/occupational-qualifications/wp-content/uploads/2019/06/Guidelines-For-Writing-of-FISA-and-EISA-2018.pdf), [QCTO: External Integrated Summative Assessment (EISA) — TrainYouCan](https://trainyoucan.co.za/qcto-external-integrated-summative-assessment-eisa/), [POPIA compliance essentials — Secure Privacy](https://secureprivacy.ai/blog/south-africa-popia-compliance).

## 3. Roles and Permissions

**Four roles** are in scope: Administrator, Learner, Assessor, Invigilator. There is no public sign-up. **Moderator and Head QA are not FPT Exam roles** — moderation and QA oversight live in the FPTStaff application (Section 4a); the earlier v1 design's Moderator and Head QA dashboards were removed on 4 Sep 2026.

**People are registered on FPT Exam by pulling them from FPTStaff.** FPTStaff is the system of record for people. An Administrator registering a user picks *what* they are registering — Student, Assessor or Invigilator — and selects the person from a searchable dropdown linked to the matching FPTStaff section; their details and role pull through. If the person is not in FPTStaff yet, the Administrator can add their details, and FPT Exam pushes the new record to FPTStaff (with a duplicate guard on ID number and email). Until the FPTStaff connection is live, the same screen is used with the details entered by hand; those records are pushed to FPTStaff in a catch-up sync once connected. Administrator accounts are internal to FPT Exam and created directly.

**Administrator**
Full system configuration authority. Builds the assessment instrument (question paper, marking guide, rubric, time allocation) for each qualification and attaches it to an exam record, via any of four intake paths — manual entry, AI-generated from the qualification's SAQA record, AI-generated from an uploaded QCTO assessment specifications document, or (for FISA qualifications) imported from Curricula Builder — see Section 5. Registers all other user accounts by pulling them through from FPTStaff as described above (role pre-filled from the FPTStaff section, adjustable). Creates exam sittings: qualification, cohort, date/time window, assigned invigilator(s), assigned assessor, proctoring settings (capture interval, recording on/off, lockdown level). Assigns learners to sittings. Views system-wide audit logs, integrity dashboards, and recording storage status. Cannot mark exams or override an assessor's sign-off — Administrator is an operations/configuration role, not an academic one, to keep the "who can influence a result" line clean for auditors.

**Learner**
Never signs up on FPT Exam — is registered once on FPTStaff and pulled through by the Administrator. Is assigned to an exam sitting. Completes mandatory pre-exam steps: identity verification, device/environment check, POPIA consent capture, and a systems check (camera, mic, screen-share permission, bandwidth). Sits the exam inside the locked-down exam environment. Can raise an in-exam flag to the invigilator (e.g. "technical issue", "need a break" per the qualification's reasonable-accommodation rules). Receives their result and per-question feedback once the Assessor has signed off. Has POPIA data-subject rights: can request a copy of their own recording/log, request correction of factual account data, and request deletion once the retention period and any dispute window has passed (Section 10).

**Invigilator**
Independent of the marking process. Monitors one or more live exam sessions in real time (video wall of learner webcams, live flag feed). Verifies learner identity at session start (ID document vs. webcam vs. registered photo). Can pause, message, or terminate a learner's session for a policy breach; logs every such action with a timestamp and reason (creates an immutable incident record). Does **not** see exam questions/answers and cannot be the Assessor of record for the same sitting — enforced by the permission system, not just convention, per Section 2. Where an exam is flagged "independent invigilation required," only accounts marked `employment_relationship = external` can be assigned.

**Assessor**
Subject-matter role. Receives, per learner, the submitted responses, the AI Integrity Report, and the AI Response-Review Report (Section 7) once the exam window closes. Marks or confirms marks against the qualification's marking guide, records a competent / not-yet-competent (or numeric mark, per qualification) outcome per assessment criterion, and either accepts, edits, or overrides every AI-generated flag or suggested mark before signing off — the system must make it structurally impossible to publish a result the Assessor has not personally reviewed and signed. **Assessor sign-off is the gate: the moment it is recorded the result is final, released to the learner, and pushed to FPTStaff.** Applies identically to FISA and EISA.

| Role | Sees exam content | Can mark | Can invigilate live | Can register users | Result sign-off? |
|---|---|---|---|---|---|
| Administrator | No (config only) | No | No | Yes (by pulling from FPTStaff) | No |
| Learner | Own paper only | No | No | No | No |
| Invigilator | No | No | Yes | No | No |
| Assessor | Yes | Yes | No | No | **Yes — the only gate** |

## 4. Exam Lifecycle

1. **Instrument intake** — Administrator builds or imports the assessment instrument (paper, marking guide, time allocation, allowed materials) via one of four paths — manual entry, AI-generated from the qualification's SAQA record, AI-generated from an uploaded QCTO assessment specifications document, or Curricula Builder import for FISA qualifications — and attaches it to a new Exam record (Section 5).
2. **Sitting setup** — Administrator configures the sitting: cohort, date/time window, duration, proctoring profile (capture cadence, full recording on/off, lockdown level, breaks allowed), assigned Invigilator(s) and Assessor, and whether independent invigilation is required.
3. **Enrolment & pre-checks** — Learners (pulled from FPTStaff) are assigned to the sitting. Ahead of the exam, each learner completes identity verification (ID document capture + liveness selfie compared against their registered profile photo), a device/browser check, and explicit POPIA consent for recording (Section 10) — a learner cannot enter the exam without all three.
4. **Check-in** — On the day, learner joins a virtual "waiting room," invigilator verifies identity live, confirms environment (room pan via webcam, desk clear), and admits the learner.
5. **Live sitting** — Learner works inside the locked-down exam environment (Section 6). Webcam stream is live to the invigilator throughout; screen captures are taken at a configurable interval (default: every 30–60 seconds) plus event-triggered captures (tab switch, paste event, window blur); full session recording (webcam + screen) is optional per sitting and, if enabled, streamed to encrypted storage continuously rather than buffered locally, so nothing is lost if the learner's device fails.
6. **Submission** — Learner submits or time expires (auto-submit). Session and recording are sealed (write-once) and the incident log for that learner is finalised.
7. **AI review** — The Integrity Engine and Response-Review Engine (Section 7) run against the sealed session: the Integrity Engine produces a flagged-events report; the Response-Review Engine produces a suggested mark/competency recommendation per criterion against the marking guide, plus a gap map by outcome. Both are attached to the learner's record as *recommendations*, clearly watermarked as AI-generated and not yet a result.
8. **Assessor review & sign-off — the gate** — Assessor sees the learner's submission, the Integrity Report, and the Response-Review Report side by side, marks (accepting/editing/overriding AI suggestions as needed), and signs off. Pass/fail (competent / not yet competent) is determined per the instrument's pass-mark or competency rule. **This is the final step inside FPT Exam:** the result (mark/competency outcome plus the assessor-confirmed per-question feedback and gap map) becomes official and visible to the Learner immediately.
9. **Push to FPTStaff** — At the same moment, FPT Exam pushes the result to FPTStaff (Section 4a): learner identity, qualification, instrument/sitting, mark, pass/fail, assessor and sign-off timestamp, evidence reference. FPTStaff takes over from here: it runs moderation of the process portfolio and triggers verification — **for learners who have passed only**. Nothing in FPT Exam waits on any of that.
10. **Retention & disposal** — Recordings and logs are retained per the policy in Section 10 and then disposed of automatically, unless a dispute or audit hold is active.

## 4a. FPTStaff Integration (people in, results out)

FPTStaff is FPT Academy's staff/QA application and the system of record for both **people** and **QA**. FPT Exam integrates with it in two directions. Both hooks are designed and built into FPT Exam from the start (Phase B); they **activate when FPTStaff can connect** (Phase E of `build-roadmap.md`), which depends on FPTStaff exposing an API or connectable database — if it does not, that side is built into FPTStaff as part of Phase E.

- **Inbound — people.** The registration dropdown (Section 3) searches FPTStaff's students / assessors / invigilators sections by name or ID number and pulls the person's details and role. Every person record in FPT Exam carries `source` (`manual` | `fptstaff`) and `fptstaff_id`.
- **Outbound — new people.** A person added via "Add new" is pushed into the matching FPTStaff section; FPTStaff returns an ID that FPT Exam stores. **Duplicate guard (required):** before saving, check FPTStaff by ID number and email and warn if a match exists — duplicate learner records cause real verification/certification problems. People registered by hand before the connection exists are pushed in a catch-up sync.
- **Outbound — results.** On Assessor sign-off (Section 4, step 9). FPTStaff then handles moderation (one sign-off on the full process portfolio, by the Moderator or delegated FPT staff — never per result or per sample) and verification, for passed learners only.
- **Login for pulled-in people** — to be decided when FPTStaff is ready to connect: single sign-on against FPTStaff credentials, or an FPT Exam-issued login on registration. Manually registered people keep the existing local login meanwhile.

## 5. Assessment Instrument Intake (four paths)

An `AssessmentInstrument` can be created four ways. All four produce the same internal object (Section 8), so everything downstream — sitting creation, the exam-taking UI, the Assessor's dossier — treats them identically regardless of source. **Every path produces the marking rubric with the paper**: each question carries a model answer / rubric criteria, a maximum mark, and (on the AI paths) the ELO/AC it assesses — this is what the Response-Review Engine (Section 7.3) marks against.

1. **Manual entry** — Administrator builds the paper question by question through a guided form: question type (MCQ / short-answer / long-answer / practical-upload), prompt, max mark, model answer/rubric criteria, time allocation, permitted materials. Always available for any qualification, as the universal fallback path. *(Built.)*

2. **AI-generated from the qualification's SAQA record** — Administrator supplies the SAQA qualification ID/code for a registered qualification (stored once against the `Qualification` record). The platform fetches that qualification's public SAQA page, extracts its Exit Level Outcomes (ELOs) and Associated Assessment Criteria (ACs), and the Instrument Generation Engine (Section 7.1) drafts a full paper mapped to those outcomes — each question tagged with the ELO/AC it addresses, so alignment is visible and auditable later. Available for either FISA or EISA qualifications, at FPT Academy's discretion. The drafted paper is usable immediately — the same as a manually-created one — so it can be attached to a sitting as soon as it's generated; an Administrator can still review and edit it, but that review isn't a system-enforced gate. Note: SAQA does not publish an official API. This path fetches SAQA's public qualification page by ID and parses the outcomes/criteria out of it, which is inherently less resilient than a real API — if SAQA changes that page's structure, the extraction step needs updating, so it should be monitored rather than assumed permanently stable. *(Built.)*

3. **AI-generated from an uploaded QCTO document** — for a qualification where FPT Academy has the actual document from its AQP rather than (or in addition to) a SAQA ID: a Qualification Assessment Specifications (QAS) document, sometimes called an External Assessment Specifications document — in practice a PDF or Word file. **Note: QCTO does not distribute these as SCORM packages.** SCORM is an e-learning content-packaging and completion-tracking standard for online courses, not an assessment-paper format; this was confirmed against QCTO's own e-assessment policy, which describes assessment content as based on a "Qualification Assessment Specifications (QAS) Addendum (Blueprint)" document with no SCORM or other packaging standard mentioned. An Administrator uploads the document; the platform extracts its text and the AI identifies the Exit Level Outcomes / Assessment Criteria within it (the document's structure isn't standardised the way SAQA's page is, so this step is AI-driven rather than heading-anchored parsing), then the same Instrument Generation Engine (Section 7.1) drafts a full paper from them exactly as it does for the SAQA path. Same "usable immediately" rule as path 2. *(Built.)*

4. **Curricula Builder import** — for FISA qualifications specifically, the finished assessment package (paper, marking guide, time allocation, permitted materials) is pulled through from Curricula Builder. Present on the Instruments screen as the fourth path from the start; **inactive until Curricula Builder exposes an API** (Phase F of `build-roadmap.md`). Until then FISA instruments use paths 1–3.

**Shared contract**: every path produces an `AssessmentInstrument` object with fields: `qualification_id`, `qualification_title`, `assessment_type` (FISA / EISA / other), `version`, `questions[]` (each with id, type [MCQ / short-answer / long-answer / practical-upload], prompt, max_mark, model_answer/rubric_criteria, and — for AI-generated questions — the ELO/AC reference it maps to), `time_allocation_minutes`, `permitted_materials[]`, `pass_mark_or_competency_rule`, and `source` (manual / ai_generated / qcto_upload / curricula_builder).

## 6. Proctoring, Security & Invigilation Architecture

- **Identity verification**: ID-document capture + liveness check at enrolment, re-verified at check-in against the live webcam feed; mismatch blocks entry and alerts the invigilator.
- **Continuous presence check**: lightweight face-presence detection on the client (runs locally in-browser, not server-side, to minimise data sent) flags "face not visible," "multiple faces visible," or "different face detected" events to the invigilator's live dashboard and to the Integrity Engine.
- **Screen capture cadence**: default periodic screenshot every 30–60 seconds (configurable per sitting), plus event-triggered captures on tab/window blur, copy/paste, and (where feasible) detection of a second monitor or virtual machine.
- **Full session recording (optional per sitting)**: continuous webcam + screen video, streamed directly to encrypted object storage in chunks (not stored client-side) so a crash or forced shutdown doesn't destroy evidence; sealed and hashed on submission to prove it wasn't altered afterward.
- **Lockdown behaviour** (browser-based, since this runs on Replit rather than a native lockdown client): full-screen enforcement with exit-attempt detection, new-tab/new-window blocking where the browser API allows it, clipboard monitoring, and a configurable allow-list of permitted URLs/apps for practical/open-book components. Be explicit with stakeholders that browser-based lockdown cannot guarantee the same level of control as a dedicated native lockdown client (e.g. a learner using a second physical device is not detectable) — this is a known, documented limitation to disclose to QCTO/AQP auditors, not something to overclaim.
- **Live invigilation console**: grid view of all live webcams in a sitting, real-time flag feed (presence, tab-switch, paste, noise-threshold, network drop), one-click pause/message/terminate per learner, and a running incident log per learner that timestamps every invigilator action.
- **Chain of custody**: every recording, screenshot, and log entry is immutable once written (append-only storage, cryptographic hash recorded at seal time) so that evidence produced for an AQP audit or a dispute can be shown not to have been altered after the fact.

## 7. AI Assessment Bot

Three distinct engines support the platform, at two different points in the lifecycle. Before any learner sits an exam, the **Instrument Generation Engine** can draft a full assessment paper — with its rubric — from a qualification's outcomes, sourced either from its SAQA record or from an uploaded QCTO document (Section 5); this is authored content, not a recommendation about a learner, so it carries no sign-off requirement of its own and is usable as soon as it's generated. Once a learner's session is sealed, the **Integrity Engine** and **Response-Review Engine** feed one combined recommendation dossier per learner, and together they do the actual assessment automatically — a suggested mark/competency outcome, per-question feedback, and a gap map by outcome for every submission, without an assessor manually marking from a blank slate. Neither of those two engines has authority to finalise a result on its own: QCTO's model puts that authority with a human, and on this platform that human is **the Assessor, alone** — every recommendation-stage AI output is explicitly labelled a *recommendation* until the Assessor actively accepts, edits, or rejects it and signs off. That single sign-off is the whole gate; moderation and verification then happen in FPTStaff on the process, not on the individual result (Section 4a). This is a deliberate design constraint, not a limitation to fix later: an AQP auditing your process needs to see a human decision trail for a learner's result, not a fully automated one — and that trail should be as short as QCTO's model actually requires.

**7.1 Instrument Generation Engine** — takes a qualification's Exit Level Outcomes and Associated Assessment Criteria (extracted either from its SAQA record or from an uploaded QCTO document — Section 5) plus the desired time allocation, and drafts a full `AssessmentInstrument`: a set of questions (mixed types — MCQ, short-answer, long-answer, practical-upload — as appropriate to the outcomes being assessed), each with a max mark, a model answer/rubric, and a tag identifying which ELO/AC it addresses. The output is stored as the instrument's `questions[]` exactly like a manually-authored one; nothing about it is presented differently to the Assessor or Learner later in the lifecycle — the AI's role here is authorship, not a recommendation about a specific learner, so it does not get folded into the Assessor sign-off gate described below. Because the resulting paper certifies real learners, FPT Academy should still expect an Administrator to skim a generated paper before scheduling a sitting against it, the same care given to a manually-built one — it's simply not a system-enforced requirement. *(Built.)*

**7.2 Integrity Engine** — reviews the sealed screen captures, recording (if enabled), and the live-session event log for a learner's sitting, and produces a written findings report: which flags fired, at what timestamp, with the supporting screenshot/clip attached, and a plain-language summary (e.g. "3 tab-switch events between 00:12–00:14, no face-visibility issues, no multiple-face detections"). Optionally proposes an overall integrity recommendation ("no concerns" / "minor — note only" / "flag for investigation") based on FPT Academy's configurable thresholds, but always shows its reasoning and the underlying evidence rather than a bare verdict. *(Phase D.)*

**7.3 Response-Review Engine** — yes, the actual submitted assessment responses are reviewed, not just the proctoring feed. This engine takes the learner's submitted answers plus the marking guide/rubric from the attached `AssessmentInstrument` (Section 5) and produces, per question/criterion: a suggested mark or competent/not-yet-competent judgement, the rubric criteria it matched or missed, a read on depth of understanding (correct but shallow / lacking justification, not just right/wrong), a confidence indicator and a plain-language rationale. Because every question is tagged to an outcome, it rolls these up into a **gap map**: which Exit Level Outcomes the learner has demonstrated and which they have not yet. For practical/portfolio-style uploads (documents, images, code, etc.) it extracts and summarises the submission against the rubric rather than marking blind. This output is explicitly a *marking aid*, presented to the Assessor next to the learner's actual submission — never shown to a Learner as a mark in its own right. *(Phase C.)*

**7.4 Combined dossier** — the Assessor's review screen shows, per learner: original submission, Response-Review suggestions inline against each answer, the gap map, Integrity Engine findings with evidence, and a sign-off panel that logs the Assessor's final decision on every AI suggestion (accepted / edited / overridden, with an optional reason). This log is part of the audit trail and travels with the result pushed to FPTStaff.

**7.5 Feedback to the Learner** — once the Assessor has signed off (Section 4, step 8), the per-question feedback from the Response-Review Engine — what was correct, what was missed against the rubric, the gap map, and the final mark/competency outcome — is released to the Learner alongside their result. The Learner never sees AI output before that point; what they see afterward is the human-confirmed version of it (the Assessor's edits/overrides, if any, take precedence over the AI's original suggestion).

## 8. Data Model (core entities)

`User` (id, name, id_number_hash, email, role[], employment_relationship, photo_reference, **source: manual/fptstaff, fptstaff_id**) — `source`/`fptstaff_id` are the FPTStaff integration hooks (Section 4a)
`Qualification` (id, title, qcto_registration_type: FISA/EISA, aqp_reference, saqa_qualification_id)
`SaqaQualificationExtract` (id, qualification_id, saqa_qualification_id, exit_level_outcomes[], assessment_criteria[], source_url, fetched_at) — the audit snapshot of the SAQA data used to draft an AI-generated instrument
`QctoDocumentExtract` (id, qualification_id, original_filename, exit_level_outcomes[], assessment_criteria[], uploaded_at) — the audit snapshot of an uploaded QCTO document (QAS/External Assessment Specifications) and what the AI identified in it, same purpose as `SaqaQualificationExtract` above but for the upload intake path
`AssessmentInstrument` (id, qualification_id, version, questions[] — each optionally tagged with the ELO/AC it maps to, marking_guide, time_allocation, source: manual/ai_generated/qcto_upload/curricula_builder, saqa_extract_id, qcto_extract_id)
`ExamSitting` (id, qualification_id, instrument_id, cohort_id, start_time, end_time, proctoring_profile, assigned_invigilators[], assigned_assessor, independent_invigilation_required: bool)
`LearnerSession` (id, sitting_id, learner_id, check_in_time, submission_time, status, consent_record_id)
`CaptureEvent` (id, session_id, type: screenshot/full_recording/system_event, storage_ref, timestamp, hash)
`IncidentLog` (id, session_id, raised_by: system/invigilator, type, timestamp, evidence_ref, action_taken)
`AIIntegrityReport` (id, session_id, findings[], overall_recommendation, generated_at)
`AIResponseReview` (id, session_id, per_question_suggestions[], gap_map[], generated_at)
`AssessorDecision` (id, session_id, per_criterion_marks[], ai_suggestions_accepted/edited/overridden[], outcome: competent/not_yet_competent, sign_off_time) — **sign_off_time being set is the release event**
`FptstaffResultPush` (id, session_id, payload, pushed_at, fptstaff_ack, status: pending/sent/failed) — the outbound handoff record (Section 4a); queued at sign-off, delivered when FPTStaff is connectable
`ConsentRecord` (id, learner_id, sitting_id, consent_text_version, accepted_at, ip_address)

*Removed from v1:* `ModerationRecord` and `ResultRelease` — moderation is FPTStaff's, and release is simply the Assessor's sign-off. (The database still carries the legacy `moderation_records` / `result_releases` tables and the `moderator`/`head_qa` enum values from v1; they are unused and left in place because removing them is destructive for no gain.)

## 9. Suggested Technical Architecture (Replit-oriented)

- **Frontend**: React (Vite) SPA with role-routed areas for Learner (exam UI), Invigilator (live console), Assessor (marking dossier) and Administrator (setup), served from the same Replit app. Design system: FPT Academy green/blue, Manrope + Public Sans, light mode only.
- **Backend**: Node.js/Express exposing REST/WebSocket APIs; WebSocket channel specifically for the live invigilation feed (flags, presence status) so the console updates in real time without polling. On start-up the server applies pending database migrations and creates the bootstrap Administrator from secrets if none exists (idempotent), so a fresh deployment needs only its secrets.
- **Database**: PostgreSQL (Replit's built-in Postgres, or an external managed instance such as Neon/Supabase if you want a longer-lived database independent of the Repl's lifecycle) for all relational data in Section 8.
- **Media capture**: browser `getUserMedia`/`MediaRecorder` APIs for webcam+screen capture; `getDisplayMedia` for screen share; chunks uploaded over WebSocket or chunked HTTP to object storage as they're recorded, not buffered fully client-side.
- **Object storage for recordings/screenshots**: Replit's own storage is not designed for large video volumes at exam scale — use an S3-compatible bucket (AWS S3, Cloudflare R2, or Backblaze B2) with server-side encryption, a documented South African-or-adequate-jurisdiction region choice for POPIA cross-border rules, and lifecycle rules matching the retention policy in Section 10.
- **AI engines**: backend calls to an LLM/vision API (Anthropic). All AI work runs as background jobs the client polls — including instrument generation, which takes one to two minutes and would otherwise be cut off by the hosting gateway's ~60s request limit. The Integrity and Response-Review engines run as jobs once a learner's session is sealed. All three write to their respective tables and never touch the Learner-facing UI directly.
- **FPTStaff integration**: an outbound client for people-push and result-push, and an inbound people lookup for the registration dropdown (Section 4a). Both are behind a single connection setting; until FPTStaff is connectable the UI shows the hooks inactive and queues outbound pushes.
- **Background jobs**: a lightweight Postgres-backed job table with a polling worker for AI review jobs, FPTStaff pushes/retries, retention/disposal jobs, and notification jobs.
- **Auth**: role-based accounts with mandatory MFA for Administrator, Assessor and Invigilator (result-affecting or supervisory roles); Learner accounts use a lighter flow but still require the identity-verification step before an exam. Login model for FPTStaff-pulled people to be decided at Phase E (Section 4a).

## 10. POPIA & Data Governance

- **Consent**: a specific, plain-language consent screen presented before a learner can enter an exam, explaining exactly what is captured (webcam, screen, ID document image), why (exam integrity, QCTO/AQP evidentiary requirement), how long it's kept, and who can access it. Recorded as a `ConsentRecord` with a timestamped, versioned copy of the text the learner agreed to — not just a checkbox flag.
- **Minimisation**: capture only what the integrity/marking use case needs — no microphone-always-on audio recording unless a specific qualification requires it; no capture of the learner's surroundings beyond what identity/environment checks require.
- **Retention**: define a default retention period (commonly 6–12 months, aligned with the qualification's remark/dispute window and any AQP audit cycle) after which recordings and screenshots are automatically deleted by a scheduled job — with an audit-hold flag that can suspend deletion for a specific session under investigation or dispute.
- **Access control**: recordings are visible only to the assigned Invigilator (own sessions), the assigned Assessor (for review), and the Learner themself (own recording, on request) — never Administrator by default (Administrator manages accounts and exam setup, not evidentiary content), unless a specific investigation role is granted. Evidence *references* travel to FPTStaff with the result; the media itself stays in FPT Exam's storage.
- **Data subject rights**: a documented process for a Learner to request access to, correction of, or deletion of their own data, honoured within a reasonable time as POPIA requires, with deletion requests checked against any active retention/dispute hold before being carried out.
- **Cross-border storage**: if using a cloud region outside South Africa, confirm the provider/region offers protection adequate under POPIA (or use a South African region where available) — this is a legal sign-off item, not a purely technical one.

This section is a design framework, not legal advice — have FPT Academy's compliance/legal function review and formally approve the consent text, retention periods, and storage jurisdiction before go-live.

## 11. Non-Functional Requirements

- **Availability during exam windows**: the platform must treat scheduled exam windows as its highest-priority uptime commitment — alerting, on-call coverage, and capacity headroom should be planned around exam dates, not general traffic patterns.
- **Graceful degradation**: a learner's connection drop should not lose captured evidence up to that point (continuous streaming upload, not end-of-session upload) and should let the invigilator resume/extend the session rather than forcing a restart.
- **Auditability**: every privileged action (role change, exam creation, sign-off, override, deletion) is logged with actor, timestamp, and reason where applicable — this log is itself evidence for an AQP audit.
- **Scalability**: architecture should handle concurrent sittings (multiple cohorts at once) without the live invigilation WebSocket channel degrading — plan capacity per concurrent learner, not just total registered learners.

## 12. Build Phases

Superseded by **`build-roadmap.md`** in the project, which is the authoritative phase plan. In summary: **A** Foundation (built) → **B** Design & model alignment (built) → **C** Assessor marking & the Response-Review engine → **D** Proctoring & invigilation → **E** FPTStaff integration goes live → **F** Curricula Builder integration.

## 13. Open Questions for FPT Academy to Confirm

- **FPTStaff connectivity** — does FPTStaff expose an API or connectable database that can supply people by section and receive new people and results? If not, that side is built into FPTStaff in Phase E.
- **Login model for FPTStaff-pulled people** — SSO against FPTStaff credentials, or an FPT Exam-issued login? Decide at Phase E.
- Which qualifications on your books are FISA (legacy) vs. EISA (QCTO-registered), since the AQP contract can differ.
- Target retention period for recordings/screenshots, and who at FPT Academy owns the POPIA sign-off.
- Whether any qualification requires audio recording, and if so, the added consent/POPIA handling that implies.
- Expected concurrent-sitting scale (how many learners at once, worst case) — this drives the storage and WebSocket capacity planning in Sections 9 and 11.
- Whether an Administrator should be required to actively review an AI-generated (SAQA- or document-upload-sourced) paper before it can be scheduled against a sitting, or whether immediate usability (the current default, Section 5) remains the right call once real qualifications are being certified through it.

---

*Sources referenced in this document: [QCTO Assessment Policy for Qualifications and Part Qualifications](https://www.qcto.org.za/assets/qctos_-policy-on-assessment-of-qualifications-(3).pdf) · [Guidelines for Writing of FISA & EISA (INSETA, 2018)](https://www.inseta.org.za/occupational-qualifications/wp-content/uploads/2019/06/Guidelines-For-Writing-of-FISA-and-EISA-2018.pdf) · [QCTO: External Integrated Summative Assessment (EISA) — TrainYouCan](https://trainyoucan.co.za/qcto-external-integrated-summative-assessment-eisa/) · [POPIA compliance essentials — Secure Privacy](https://secureprivacy.ai/blog/south-africa-popia-compliance) · [General Principles and Minimum Requirements on E-Assessment — QCTO](https://www.qcto.org.za/assets/revisedpolicyone-assessment.pdf)*
